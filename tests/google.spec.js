import {test,expect} from '@playwright/test'

test("@Google UI check",async({page})=>{
//Launch google
    // await page.goto('https://www.google.com/')

    // await page.locator('a[aria-label*="Gmail"]').isVisible()
    console.log("hello, its my first docker test")


})